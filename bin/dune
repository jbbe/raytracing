(executable
    (name flash_sphere)
    (modules flash_sphere)
    (libraries raytracing))

(executable
    (name scene)
    (modules scene)
    (libraries raytracing))

(executable
    (name plane_scene)
    (modules plane_scene)
    (libraries raytracing))